# PC端双栏PDF刷题阅读器 · 「高价值·高成本」象限 方案优化

> 范围：只针对优化优先级矩阵的**右上格**（价值明确、但实现面大／技术不确定）。
> 左下、右下两格不在本文讨论范围内。

---

## 0. 关于象限内容的复原说明

原始四象限矩阵是在评审对话里给出的，没有落盘；本次重新检索历史会话未取回逐字原文。
下面按当时的评审结论（已记录在项目工作日志）复原右上格的候选清单：

1. 文本层与双侧搜索（选词 / 复制 / Ctrl+F）
2. 目录联动（`getOutline` → 双侧跳转）
3. 文件配对记忆 + 启动页
4. 标注体系补全（高亮 / 下划线 / 矩形 + 双向关联）
5. 错题本（框选截图 + 归档 + 导出）
6. OCR（扫描版）

**如果右上格实际还包含别的项，告诉我，我按同样的方法补进来。**

---

## 1. 先做「象限校正」：把装错格的东西挪出去

在谈怎么降价之前，先承认一件事：**这一格里至少三项的归类是错的。**

| 项 | 原判 | 校正 | 理由 |
|---|---|---|---|
| 目录联动 | 高成本 | **实为低成本** | 跳转能力已具备：`viewer.apis[side].gotoPage()` + SyncEngine 映射都在跑；只差 `PdfDocument.getOutline()` 一层封装 + 一个树形列表。**半天级** |
| 鼠标侧键 | （原 P2／右下） | **低成本** | `mousedown` 判断 `button===3/4` 映射到**已有的** shortcut action，十几行 |
| 标注「双向关联」 | 高成本 | **砍掉** | 用户已能用画笔 + 文本框覆盖需求；双向关联要建立题↔解页级映射表并维护，成本/价值严重不匹配 |
| 多标签页后台休眠 | 高成本 | **砍掉** | 与「一套题一窗口」的产品心智冲突；带来页面管理、状态隔离、休眠调度三份复杂度 |
| SQLite 迁移 | 高成本 | **挂起** | 触发条件：单目录 JSON > 50MB 或标注条目 > 5 万条。§13.5 已定「痛点出现前不做」 |

校正后，这一格真正要攻的是 **5 件事**：

> 文件重定位（技术债，前置） → 文本层与搜索 → 文件配对记忆 → 错题本 → OCR

---

## 2. 五个攻坚项：成本高在哪，怎么砍下来

### 2.1 文件重定位（技术债 —— 必须前置）

**为什么归到这一格**：书签、标注、遮罩、进度四份数据的 key **全是绝对路径**。跨机、挪目录、改文件名 → 全部失效。而「配对记忆」「错题本」会把这个偶发问题放大成默认状态：不先修它，后面两项做出来就是坏的。

**成本高在哪**：需要一套「路径失效后还能找回文件」的机制。

**砍法**：**换 key + 加指纹**，不做复杂的书库索引。

- 数据里新增 `fileId`，**不再用路径当 key**：`fileId = size + hash(文件首 64KB)`
  - `fileStat` 已存在（返回 `size` / `mtime`），主进程再加一个 `file:fingerprint` 返回首段哈希即可
- 路径降级为「上次已知位置」的 cache：
  1. 先按 cache 路径打开
  2. 失败 → 在该文件所在目录（或用户指定的书库目录）下按 `fileName + size + 指纹` 扫描匹配
  3. 命中 → 静默改写 cache 路径，用户无感
- **迁移**：一次性读取旧 JSON，按当前路径补算指纹；算不出来的条目原样保留但标 `stale`，不清空

**成本**：主进程 +1 个 IPC；新增 `src/core/fs/paths.ts`；4 个 store 的 key 从 `path` 换成 `fileId`——
改动机械但集中，是这一格里**唯一必须最先做**的事。

---

### 2.2 文本层与双侧搜索

**成本高在哪（三处，且第三处才是真坑）**：

1. 文本层定位：canvas 与文本 div 必须像素级对齐（含 DPI、缩放、页内 padding）
2. 搜索：跨两侧文档、结果高亮、落点精确
3. **与已有三层的命中测试冲突**：遮罩 canvas、笔迹 canvas、文本框 DOM 都要接收鼠标，文本层还要接收选区 —— 谁吃事件，是整个功能最花时间的地方

**砍法：分两刀，第一刀完全不碰文本层。**

**第一刀（低成本，先做）**

- `PdfDocument` 加 `getTextContent(pageIndex)` → 拼 `items[].str`（注意 `hasEOL` 决定是否补换行）
- **搜索**：`Ctrl+F` 开一条搜索栏，逐页**惰性提取 + 缓存**，结果流式返回（先给前 20 条）
- 点击结果 → `gotoPage()` + 用该文本项的 `transform` 换算出页内纵向偏移 → 精确落点
- **复制**：不做选词，做「复制整页文本」+「复制结果所在行」
- 命中冲突：**不存在**（文本层还没进场）

**第二刀（做精确选词 / 高亮时才做）**

- 用 pdf.js **内置 `TextLayer`**（已确认 `pdfjs-dist@6.2.108` 导出 `TextLayer` / `setLayerDimensions`），**不要手写 span 布局**
- 层级：pdf canvas < 遮罩 canvas < **文本层** < 笔迹 canvas < 文本框
- **关键开关**：文本层的 `pointer-events` 跟随 `activeTool`——`browse` 时 `auto`，`pen` / `text` 时 `none`。
  这一条定下来，冲突问题基本就没了
- 高亮：用选区 `Range` 的 `getBoundingClientRect` 取矩形 → 按**已有的「页面基础坐标」约定**存储 → 渲染成覆盖 `<div>`，不写回 PDF

**验收切分**：搜索可独立验收、独立上线；文本层与高亮作为第二步，且**只在「复制」被确认为真痛点时才做**。

---

### 2.3 文件配对记忆（不做启动页）

**成本高在哪**：启动页是**一整套新的首屏 UI + 状态管理**；自动配对规则又因命名太发散而覆盖不全。

**砍法**：把「启动页」换成「打开即续」。

- `viewer.open('question', p)` 成功后查 `pairs.json`
- 命中 → 自动打开解析 + 恢复两侧阅读进度 + 弹一条**可撤销**的提示条（「已自动打开配套解析 · 换一个」）
- 触发条件收紧：**仅当右侧为空时**才自动，已有解析绝不覆盖
- `pairs.json` 的 key 用 `fileId`（依赖 2.1）
- **自动配对规则不做**（`XXX_题本/_解析` 覆盖不了真实命名）；改为「**最近配对记忆**」——谁配过谁，已覆盖 90% 场景
- 「历史列表」的需求由 **`BookmarkPanel` 代替**：它已经存了成对路径 + 页浮点 + 「当前」标记，本身就是历史入口。
  真要做「启动页」，下一步把它做成侧栏的一个「最近」tab，**而不是新建首屏**

**成本变化**：从「首屏 + 路由 + 配对算法」降到「一个 if + 一张表 + 一条提示条」。

---

### 2.4 错题本

**成本高在哪**：框选交互（跨两侧）+ 截图合成（含标注层）+ 二进制存储 + 归档 UI（分类/标签/搜索）+ 导出（PDF/zip）。
这是一个**完整的小应用**，不是一个功能点。而且它与「书签」高度重叠 —— 用户现在已经在用「错题」标签了。

**砍法：不新建模块，把「错题」升级为「带快照的书签」。**

- **数据模型**：`Bookmark` 增加
  ```ts
  snapshot?: { side: 'question' | 'answer'; page: number; rect: Rect }
  ```
  **只存坐标，不存图**（复用现有「页面基础坐标」约定，缩放无关）
- **框选交互**：复用 `PdfPage.vue` 已有的坐标换算；新增第 4 个 `activeTool = 'crop'`，单侧拖矩形。**只做单侧**（先题本），双侧框选砍掉
- **截图按需生成**：导出时才把「源页 canvas + 遮罩 + 笔迹 + 快照 rect」合成裁剪。
  不预存 PNG → 省存储、省 IPC
- **归档 UI：零新增** —— `BookmarkPanel` 的标签筛选就是错题本，卡片多一行缩略图
- **导出**：先做「导出当前筛选结果为 PNG 多张 / zip」；**PDF 导出（pdf-lib）延后**（独立成本，与核心价值无关）
- **存储**：这里是唯一需要二进制的地方，但**不必改存储层**。
  `writeData` 目前是纯文本；导出是用户主动动作，直接
  `dialog.showSaveDialog` + **主进程 fs.writeFile** 即可，渲染进程完全不碰二进制。

**结论**：错题本 = 书签 + 一个裁剪框 + 一个导出动作。**不需要二进制存储，也不需要新模块。**

---

### 2.5 OCR

**成本高在哪**：wasm + 语言包（`chi_sim` 15~30MB）+ 分钟级耗时 + 进度/取消 UI + 结果缓存。

**砍法**：

- **只做「当前页按需 OCR」**：不做整本、不做后台预跑
- 跑在**主进程 / `utilityProcess`**（Node 侧跑 tesseract.js，不占渲染线程）；渲染侧只发「识别第 N 页」+ 收进度事件
- 语言包**随安装包内置**（electron-builder `extraResources`），不做首次联网下载 —— 离线是这产品的前提
- **关键设计**：OCR 输出与 `getTextContent()` **同构**（产出 `TextItem[]`）。
  这样 2.2 做的搜索/复制/跳转**自动获得 OCR 能力** —— OCR 不再是独立功能，而是「文本层的第二个数据源」
- 缓存：`ocr/<fileId>/<page>.json`（又一批 JSON，无需二进制）
- 验收标准写死：单页 ≤ 10s 出结果（先做灰度 + 400dpi 预处理）

**引擎选型（2026-09 核实各家官方定价页）**

免费 OCR 云 API 确实存在且不少，但**不建议作为主路径**。

| 路线 | 代表 | 免费额度 | 判断 |
|---|---|---|---|
| 本地 wasm | Tesseract.js | 无限制、离线 | **默认引擎**：印刷体够用、零依赖、零隐私风险 |
| 通用云 OCR | 腾讯云通用印刷体 | 1000 次/月（每月 1 号发放，**当月有效**） | 备选。刷题是连续大量场景，额度太薄 |
| | 百度智能云标准版 | 个人实名 1000 次/月（**失败调用也计费**） | 同上 |
| | 阿里云视觉智能 | 约 200 次/月 | 太少，不适用 |
| | Google Cloud Vision | 1000 次/月 | 需境外网络与结算 |
| 多模态大模型 | 智谱 GLM-4V-Flash | **完全免费**（限并发 1~10） | **手写 / 公式 / 复杂版面**明显强于传统 OCR |
| | 硅基流动 DeepSeek-OCR | 实测零扣费 | 同上 |
| | PaddleOCR 官方 API | 3000 页/日 | 量大，适合批量任务 |

**为什么不做成主路径**

1. **离线是这产品的前提**（本地 pdfjs worker、本地语言包、不走 CDN），云 OCR 断网即废；
2. **与老板键「低调 / 不外泄」的定位冲突**，且免费档普遍保留「用提交数据训练」的权利；
3. 额度是**按月**而不是按页 —— 一本扫描教辅 300 页，1000 次/月几本就见底，而刷题恰恰是连续大量场景；
4. 免费档有限流（并发 1~10），一次几十页会卡；免费政策还会变，等于把核心能力押在别人的运营策略上。

**但要给云端留一个「可选增强」的位置**：中文手写体、数学公式、多栏表格，传统 OCR 打不过多模态大模型。

**架构上这是零成本的** —— 因为前面已定「OCR 输出与 `getTextContent()` 同构」，换引擎只是换一层 backend：

```ts
interface OcrEngine {
  id: 'tesseract' | 'cloud'
  recognize(page: { canvas: HTMLCanvasElement; pageIndex: number }): Promise<TextItem[]>
}
```

**结论**：先按本地引擎把 `TextItem` 管线搭完，云端引擎后面插进去即可 —— **不要为了等某个免费 API 而拖延这一项**。

**若真要走云，两条硬约束**

- API Key **绝不能打进安装包**（Electron 可被反编译），只能由用户自行填写，或走自建中转；
- 图片需按 API 限制预压缩（普遍 1~4MB、长边 ≤4096），400dpi PNG 会超限，需转 JPEG。

---

## 3. 这一格的执行顺序

| 序 | 项 | 依赖 | 量级 |
|---|---|---|---|
| 1 | **文件重定位**（技术债） | — | 中，但解锁后面两项 |
| 2 | **目录联动** + 鼠标侧键 | — | 半天级，先摘最便宜的 |
| 3 | **搜索**（第一刀，不碰文本层） | — | 中 |
| 4 | **配对记忆**（无启动页） | 1 | 小 |
| 5 | **错题本**（= 书签 + 裁剪 + 导出） | 1 | 中 |
| 6 | 文本层精确选词 / 高亮（第二刀） | 3 | 中大，按需 |
| 7 | **OCR** | 3 的接口 | 大，可最后 |

依赖只有两条：**1 → {4, 5}**，**3 的接口 → 7**。其余都可并行。

---

## 4. 砍掉 / 挂起的项与触发条件

| 项 | 处置 | 重新评估的触发条件 |
|---|---|---|
| 标注「双向关联」 | 砍 | 出现「必须从题跳到解的具体位置」的真实抱怨 |
| 多标签页后台休眠 | 砍 | 产品定位从「一套题一窗口」改为「多套题并行」 |
| SQLite 迁移 | 挂起 | 单目录 JSON > 50MB 或标注条目 > 5 万 |
| 启动页 | 挂起 | 配对记忆上线后仍有人找不到历史题组 |
| 自动配对规则 | 挂起 | 「最近配对记忆」命中率不足 |
| 错题本 PDF 导出 | 挂起 | PNG 导出被实际使用且明确不够 |
| 文本层精确选词 | 挂起 | 「整页复制」被证明不够用 |
| 手写 span 布局文本层 | **永不做** | 用内置 `TextLayer` |

---

## 5. 补充建议（超出右上格范围，但优先级更高）

### 5.1 ⚠️ 方案漏了优先级最高的一项：同步口径

> **✅ 已修复（本节落地结果）**
> - `RatioStrategy` 改为真正的「滚动高度百分比」：`SyncStrategy.map(pf, src, dst)`，两侧各自传入
>   `SyncSide { mapper, viewportHeight }`，映射在进度空间做（`src` 页浮点 → 进度 → `dst` 页浮点）。
> - 同时补上 **pageScale**（倍率+偏移，可持久化）与 **锚点校准**（会话内，一键对齐 `Ctrl+Alt+A`），
>   三种方式在工具栏「⋮」→「同步方式」切换。
> - 数值自测（题本 186 页 / 解析 240 页）：旧口径末端错位约 **54 个解析页**；新口径两侧进度偏差
>   **0.031 页**（来源是页间距 `gap` 的量化上限，见 `技术设计文档.md` §11 难点表），映射严格单调。
> - ⏳ 仍未做：**锚点持久化**（按本文结论，等 2.1 的 fileId）。

**它不在任何象限里** —— 因为它是「核心功能正确性」，不是新功能。但它是这产品存在的理由。

现状（已核对代码）：

```ts
// src/core/sync/SyncEngine.ts
export class RatioStrategy implements SyncStrategy {
  map(pf: number) { return pf }   // ← 页浮点恒等，不是比例
}
```

`PageMapper` 的「页浮点」= 0-based 页索引 + 页内比例，所以 `map` 恒等 = **按页码同步**，不是按比例同步。

**后果**：题本 186 页 / 解析 240 页（正是产品预设的真实场景）时，滚到题本第 93 页，解析也停在第 93 页，
而正确位置约在第 120 页 —— **误差随进度线性累积，越往后越对不上**。
`技术设计文档.md` §6.2.1 定义的「滚动高度百分比」天然处理页数差异，代码与文档口径不一致。

**为什么它比右上格所有项都紧急**：刷题时对不上答案 = 产品没用。文本层、错题本、OCR 都建立在这个地基上。

**拆分与顺序（关键）**：

| 子项 | 依赖 | 能否立刻做 |
|---|---|---|
| **口径修正**（改为滚动高度百分比，需把两侧 `viewportH` 传进核算） | 无，纯算法 | **能，立刻** |
| `pageScale`（倍率 + 起始偏移） | 无 | 能 |
| 锚点一键对齐（**会话内**） | 无 | 能 |
| 锚点**持久化**（`anchors.json`，key = fileId 对） | **依赖 2.1 的 fileId** | 等 fileId |

→ 所以正确顺序是：**先修口径 → 再做 fileId → 最后锚点持久化**。
不要让同步修正去等 fileId，两者可以并行。

**验收**：用 `scripts/` 里已有的 PDF 造一对**页数不同**的文件，分别在 0% / 50% / 100% 处检查两侧是否落在同一道题上。

### 5.2 数据文件要现在就加版本号（和 fileId 改造同一批做）

`bookmarks.json` / `annotations.json` / `masks.json` / `progress.json` 现在都是**裸数组**，没有版本字段。

而右上格要做的事（fileId 改造、`Bookmark.snapshot`、`anchors.json`）是**第一次真实的数据结构变更**。
这次不打外壳，以后就只能写「猜格式」的兼容代码。

建议：统一改成 `{ version: 1, items: [...] }` + 一个 `migrate(raw)` 入口，所有读取都先过一遍。
成本极低，收益是「以后每次改模型都不用担心旧数据」。

### 5.3 加单实例锁（几行，防数据被整份覆盖）

已确认 `electron/main.ts` **没有 `app.requestSingleInstanceLock()`**。

开两个窗口 → 两个进程写同一批 JSON。而写入是**全量重写**，所以后触发的窗口会把另一个窗口的标注 / 书签
**整体盖掉**。`DataStore` 的串行队列只在**单进程内**有效，跨进程完全不生效。

修复：几行拿到锁，第二次启动时聚焦已有窗口。属于「数据安全」类，建议和 2.1 一起做。

### 5.4 把「导出」抽成通用能力，别让错题本自己带一套

错题本导出、以后的标注图层导出、整页截图导出，都是同一条路：**渲染合成 → 主进程保存**。

建议先写一个通用 `Exporter`（输入：页号 + 坐标系 + 是否含遮罩/笔迹；输出：PNG / zip；走主进程 `showSaveDialog`）。
这样错题本只是它的**第一个调用方**，而不是它的实现者。

### 5.5 验收标准要写进方案，并复用已有的冒烟钩子

方案目前定了「做什么」，没定「怎么算做完」。

项目已有冒烟机制（`PDFVIEW_SMOKE=1` + dev 模式暴露 store），但主进程里内嵌了约 670 行脚本。
**趁右上格要动主进程之前，先把冒烟脚本抽到 `scripts/smoke/`**，然后每个新功能顺手加一条用例。
先抽干净再加，比堆完再抽便宜得多；否则功能越加越多，回归只能靠手点。

### 5.6 产品判断：「文本层精确选词」可能是伪需求

刷题场景真正需要的是**在解析里搜题干关键词**（「这题考的是××定理，答案哪页讲了」），
而不是「选中一段文字复制到别处」。

方案里已把搜索和选词切开，建议再进一步：**先只做搜索**，选词复制等真实抱怨出现再上。
（目录联动相反 —— 分类汇编类教辅的目录很有价值，支持先做。）

### 5.7 元建议：右上格做完，建议停止扩功能，转一轮「质量轮」

两轮象限已覆盖高价值·低成本与高价值·高成本，再往下都是低价值条目 —— 继续做只会让维护面变大、收益递减。

建议右上格落地后进入**质量轮**：修同步口径、补最小测试（`PageMapper` 往返换算 / `DataStore` 串行与去抖）、
性能复核（`viewer.open()` 逐页串行 `getPageCssSize`、`PdfPage.render` 不 cancel `RenderTask`）、加 lint。

放在 2.1（数据层改造）之后做最划算 —— 那时数据层刚动过，测试才写得准。

---

## 6. 一句话结论

右上格的成本，**有相当一部分不是「技术难」，而是「挂载点选错了」**：

- 错题本去**新建模块** → 应该**升级书签**
- 配对记忆去**建启动页** → 应该做**「打开即续」**
- OCR 去**做成独立功能** → 应该做成**文本层的第二数据源**
- 目录联动被当成大工程 → 其实**基建都已就位**

换掉挂载点之后，这一格的工程量从「5 个新模块」降到 **1 个前置改造 + 4 处扩展**，
且其中两项（目录联动、鼠标侧键）**当天就能落地**。
