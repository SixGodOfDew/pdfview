# PC端双栏PDF刷题阅读器 · 技术设计文档

> 版本：v0.1（评审稿）
> 目标平台：仅 Windows
> 技术栈：Electron + Vue 3 + TypeScript + PDF.js + Pinia + electron-vite

---

## 1. 项目概述

本应用是一个**双栏 PDF 刷题阅读器**：左侧打开「题本」，右侧打开「解析」，两者按刷题节奏联动。核心价值是**双 PDF 之间的位置映射与同步**，以及围绕「做题不看答案」场景的遮罩、书签、错题等能力。

### 1.1 范围分级

| 级别 | 内容 |
|------|------|
| **P0 MVP** | 双栏打开、比例同步、主从模式、缩放同步、解析遮罩（点击/悬停/橡皮擦）、书签、分页懒加载、主题 |
| **P1 二期** | 倍率+偏移同步、一键对齐、目录/搜索联动、分栏拖拽、快捷键、文件配对记忆、专注模式、草稿板 |
| **P2 进阶** | 双侧标注、错题本、OCR、阅读进度统计 |

### 1.2 核心抽象

整个系统围绕「两个 PDF Viewer 之间的**页浮点坐标映射**」构建。所有联动功能（同步滚动、目录跳转、搜索跳转、书签）最终都归结为：**把一侧的页浮点坐标，经某种策略映射为另一侧的页浮点坐标**。

---

## 2. 技术选型

| 领域 | 选型 | 说明 |
|------|------|------|
| 桌面框架 | Electron（最新稳定版） | 自带 Chromium，跨平台一致，与 Web PDF 生态无缝 |
| 前端框架 | Vue 3（`<script setup>` + Composition API） | 用户指定 |
| 语言 | TypeScript 5.x | 全链路类型安全 |
| 构建工具 | **electron-vite**（最新稳定版） | 官方推荐的 Electron+Vue+TS 一体化脚手架，支持主/预加载/渲染三进程热更新 |
| 状态管理 | Pinia | Vue 3 官方推荐，轻量 |
| PDF 引擎 | **pdfjs-dist**（4.x） | 纯 JS/WASM，原生 textLayer / annotationLayer，满足搜索/目录/标注/OCR 扩展 |
| 持久化 | electron-store（本机偏好）+ JSON 数据文件（业务数据） | 业务数据放可自定义「数据目录」，原子写 JSON，支持导出/导入跨机同步 |
| 打包 | electron-builder | Windows NSIS 安装包 |
| UI 组件 | 手写为主 + 少量无样式工具库 | 「沉浸式极简界面」需求下，避免重型组件库 |

### 2.1 选型理由（PDF 引擎）

PDF.js 相对 PDFium / MuPDF 的决定性优势：原生提供 **textLayer（文本层，支撑全局搜索/复制）** 与 **annotationLayer（注释层，支撑标注）**。清单中的「目录/搜索联动」「OCR」「标注」都依赖文本层，若选 PDFium/MuPDF 需自行封装大量能力。

PDF.js 的渲染性能短板通过**分页虚拟滚动 + Web Worker 解析**弥补，数百页文档仍可秒开。

---

## 3. 进程与整体架构

```
┌───────────────────── Electron 主进程 ─────────────────────┐
│  窗口创建与管理 · 原生菜单 · 全局快捷键 · 文件对话框      │
│  · 持久化(electron-store: 本机偏好) · 打包               │
└──────────────────────────────┬───────────────────────────┘
                               │ IPC (contextBridge 安全封装)
┌────────────────────────── 渲染进程 ──────────────────────┐
│                                                            │
│  ┌────────────┐   SyncEngine    ┌────────────┐             │
│  │ PdfViewer  │◄── 页浮点映射 ──►│ PdfViewer  │             │
│  │  左:题本    │                 │  右:解析    │             │
│  └─────┬──────┘                 └─────┬──────┘             │
│        │  MaskLayer / AnnotationLayer  │                    │
│        └──────────────────────────────┘                    │
│  ┌──────────────────────────────────────────────┐          │
│  │ UI 层：工具栏 · 书签面板 · 目录面板 · 搜索面板  │          │
│  │        · 草稿板 · 主题 · 分栏拖拽              │          │
│  └──────────────────────────────────────────────┘          │
│  ┌──────────────────────────────────────────────┐          │
│  │ 状态层(Pinia)：viewer×2 · sync · mask ·      │          │
│  │              bookmark · settings · pair      │          │
│  └──────────────────────────────────────────────┘          │
│  ┌──────────────────────────────────────────────┐          │
│  │ 存储层：JSON 数据文件(数据目录)               │          │
│  └──────────────────────────────────────────────┘          │
└────────────────────────────────────────────────────────────┘
```

**关键设计原则**：`core/` 目录下的核心逻辑（SyncEngine、PageMapper、MaskManager）与 Vue/UI 完全解耦，纯 TypeScript 类，便于单元测试与复用。

---

## 4. 目录结构

```
pdfview/
├─ electron/
│  ├─ main.ts                 # 主进程入口，创建 BrowserWindow
│  ├─ preload.ts              # contextBridge 暴露安全 API
│  ├─ ipc/
│  │  ├─ file.ts              # 打开文件/读取元数据/批量导入
│  │  ├─ store.ts             # electron-store 读写（本机偏好）
│  │  └─ shortcut.ts          # 全局快捷键
│  └─ window.ts
├─ src/
│  ├─ main.ts                 # Vue 入口
│  ├─ App.vue
│  ├─ core/                   # ★ 与 UI 无关的核心逻辑（可单测）
│  │  ├─ sync/
│  │  │  ├─ SyncEngine.ts     # 同步引擎（核心）
│  │  │  ├─ strategies.ts     # 比例/倍率/锚点 三策略
│  │  │  └─ PageMapper.ts     # scrollTop ↔ 页浮点 双向换算
│  │  ├─ mask/
│  │  │  └─ MaskManager.ts    # 遮罩擦除状态与绘制
│  │  └─ pdf/
│  │     ├─ PdfDocument.ts    # pdfjs 封装：加载/渲染/文本/目录
│  │     └─ PdfWorker.ts      # Web Worker 配置
│  ├─ components/
│  │  ├─ PdfViewer/
│  │  │  ├─ PdfViewer.vue     # 单栏查看器（可复用×2）
│  │  │  ├─ PdfPage.vue       # 单页渲染（虚拟滚动单元）
│  │  │  └─ MaskCanvas.vue    # 遮罩 canvas 层
│  │  ├─ SplitPane.vue        # 可拖拽分栏容器
│  │  ├─ Toolbar.vue          # 顶部工具栏（自动隐藏）
│  │  ├─ BookmarkPanel.vue
│  │  ├─ OutlinePanel.vue
│  │  ├─ SearchPanel.vue
│  │  ├─ ScratchPad.vue       # 侧边草稿板
│  │  └─ PairLauncher.vue     # 启动页（历史配对）
│  ├─ stores/
│  │  ├─ viewerStore.ts
│  │  ├─ syncStore.ts
│  │  ├─ maskStore.ts
│  │  ├─ bookmarkStore.ts
│  │  ├─ pairStore.ts
│  │  └─ settingsStore.ts
│  ├─ composables/
│  │  ├─ useVirtualScroll.ts
│  │  ├─ useShortcut.ts
│  │  └─ useTheme.ts
│  ├─ storage/                # 数据文件存储与合并（原子写 JSON）
│  │  ├─ DataStore.ts         # 读写 + 原子写
│  │  ├─ merge.ts             # 导入合并（id + updatedAt 取新）
│  │  ├─ backup.ts            # 导出/导入数据包 zip
│  │  └─ locate.ts            # 文件重定位（path 失效 → 按文件名搜索）
│  ├─ types/
│  │  └─ index.ts             # 全部领域类型
│  └─ assets/
├─ electron.vite.config.ts
├─ electron-builder.yml
├─ package.json
└─ tsconfig.json
```

---

## 5. 领域数据模型（types/index.ts）

```typescript
// —— 文件定位引用（跨机路径重定位的关键）——
export interface FileRef {
  path: string;      // 最后成功定位的绝对路径（换机后可能失效）
  fileName: string;  // 文件名，重定位依据
}

// —— 文件配对 ——
export interface FilePair {
  id: string;
  name: string;             // 配对显示名
  question: FileRef;        // 题本
  answer: FileRef;          // 解析
  createdAt: number;
  lastOpenedAt: number;
}

// —— 书签 ——
export type BookmarkTag = '错题' | '重点' | '存疑';

export interface Bookmark {
  id: string;
  // P0 采用冗余文件字段（不依赖配对体系）：换文件后书签仍能定位所属文件
  questionPath: string;      // 题本绝对路径
  questionName: string;      // 题本文件名
  answerPath: string;        // 解析绝对路径
  answerName: string;        // 解析文件名
  questionPage: number;      // 0-based 页浮点
  answerPage: number;        // 0-based 页浮点
  tag?: BookmarkTag;
  note?: string;
  createdAt: number;
  updatedAt: number;
}

> 点击书签时校验当前打开文件是否与书签关联一致：一致直接双侧跳转；不一致**自动切换**——直接打开书签关联的题本/解析（加密 PDF 续接密码流程）并跳转，无需确认。P1 引入 FilePair 后可迁移为 `pairId` 关联。

// —— 阅读进度 ——
export interface Progress {
  pairId: string;
  questionPage: number;
  answerPage: number;
  updatedAt: number;
}

// —— 同步设置 ——
export type SyncMode = 'ratio' | 'pageScale' | 'anchor';
export type MasterSide = 'question' | 'answer';

export interface Anchor { qPage: number; aPage: number; }

export interface SyncSettings {
  mode: SyncMode;
  ratio: number;            // pageScale 模式倍率
  offset: number;           // pageScale 模式起始偏移（页）
  anchors: Anchor[];        // anchor 模式锚点表
  master: MasterSide;       // 主从方向
}

// —— 应用设置 ——
export type ThemeId = 'day' | 'night' | 'warm' | 'print';
export type LayoutId = 'split' | 'questionOnly' | 'answerOnly';

export interface AppSettings {
  theme: ThemeId;
  zoomSync: boolean;        // 双侧缩放是否联动
  layout: LayoutId;
  autoHideToolbar: boolean;
  shortcuts: Record<string, string>;
}

// —— 遮罩 ——
export interface MaskStroke {
  type: 'circle' | 'polyline';
  points: { x: number; y: number }[];  // 相对页面的坐标（与缩放无关）
  radius?: number;
}
export interface PageMask { page: number; strokes: MaskStroke[]; }
```

---

## 6. 核心模块设计

### 6.1 PageMapper —— 滚动坐标 ↔ 页浮点坐标

这是同步引擎的**地基**。PDF.js 每页高度由 `viewport(scale)` 决定，配合固定页间距 `gap`，可算出每页在滚动容器内的绝对偏移。

```typescript
class PageMapper {
  pageTops: number[];   // 每页顶部在容器内的 y 偏移（累计）
  pageHeights: number[];

  rebuild(heights: number[], gap: number): void {
    let acc = 0;
    this.pageHeights = heights;
    this.pageTops = heights.map(h => { const t = acc; acc += h + gap; return t; });
  }

  /** 滚动像素 → 页浮点（0-based，含页内比例） */
  scrollTopToPageFloat(scrollTop: number): number {
    // 二分查找 scrollTop 所在页
    const i = binarySearch(this.pageTops, scrollTop);
    const top = this.pageTops[i];
    const frac = clamp((scrollTop - top) / this.pageHeights[i], 0, 1);
    return i + frac;
  }

  /** 页浮点 → 滚动像素 */
  pageFloatToScrollTop(pf: number): number {
    const i = Math.floor(pf);
    const frac = pf - i;
    return this.pageTops[i] + frac * this.pageHeights[i];
  }

  get totalHeight(): number;   // 容器总高度
}
```

**为什么用「页浮点」作中间坐标**：目录、书签、搜索命中天然是「页号」；而比例同步天然是「滚动百分比」。页浮点坐标能同时无损表达两者，是三个同步策略的统一语言。

---

### 6.2 SyncEngine —— 同步引擎（P0 核心）

```typescript
class SyncEngine {
  master: MasterSide;
  private strategy: SyncStrategy;

  /** 主窗口滚动时调用（同步执行，无节流——scroll 事件已按帧派发） */
  onMasterScroll(pf: number) {
    if (!this.enabled || !this.applyToSlave) return;
    this.applyToSlave(this.strategy.map(pf));
  }
}
```

#### 6.2.1 三种同步策略

统一接口：`interface SyncStrategy { map(pf: number): number }`

**① 比例同步（默认，零配置）**
```typescript
class RatioStrategy implements SyncStrategy {
  // 基于滚动高度百分比，而非页浮点
  mapProgress(p: number) { return p; }   // 0..1 等比例
}
```
> 实现说明：比例模式在 `SyncEngine` 里走「滚动高度百分比」分支——`p = scrollTop / (totalHeight - viewportH)`，目标侧 `scrollTop = p × (targetTotalH - targetViewportH)`。天然兼容「解析比题目长」。

**② 页码倍率 + 起始偏移（pageScale）**
```
目标页浮点 = (源页浮点 × ratio) + offset
```
- 例：解析是题本 1.6 倍、题本第 1 页对应解析第 3 页 →
  `ratio = 1.6`，`offset = 3 - 1×1.6 = 1.4`（0-based 页浮点，UI 层换算为 1-based 显示）。
- **自动估算**：用户对齐首尾页 `(q1,a1)`、`(q2,a2)` 后：
  `ratio = (a2 - a1) / (q2 - q1)`，`offset = a1 - q1 × ratio`。

**③ 锚点校准（一键对齐）**
```typescript
class AnchorStrategy implements SyncStrategy {
  anchors: Anchor[];  // 按 qPage 升序
  map(pf: number): number {
    // 找相邻锚点区间，线性插值；两端外推
    const [left, right] = findInterval(this.anchors, pf);
    const t = (pf - left.qPage) / (right.qPage - left.qPage);
    return left.aPage + t * (right.aPage - left.aPage);
  }
  addAnchor(qPage: number, aPage: number) { /* 追加并保持有序 */ }
}
```
- 「一键对齐」= 把当前两侧页浮点作为新锚点 `addAnchor`。从前往后刷题时，锚点越积越多，后续对齐精度逐步提升，完全符合线性刷题习惯。

#### 6.2.2 主从模式

- `master` 决定监听哪一侧的滚动事件；只有主窗口滚动才触发同步。
- 从窗口滚动时不触发反向联动（满足「对答案时滑动解析不打乱题本」）。
- 支持切换主从方向（反转 master 即可）。

#### 6.2.3 防抖动与防回环

- **不额外节流**：浏览器 scroll 事件本身按渲染帧派发（每帧最多一次），同步应用即达到帧级节流且更即时（P0 实测 rAF 节流在 Electron 中存在调度不确定性，已弃用）。
- **主从单向监听天然防回环**：只有主侧滚动触发同步，从侧滚动被忽略，程序性 `setScrollTop` 无需标志位。
- **程序性跳转**（同步/书签/目录）在 `applyPageFloat` 内显式更新 pageFloat 与虚拟滚动视口，不依赖原生 scroll 事件回传（程序设置 scrollTop 不一定派发原生事件）。

---

### 6.3 MaskManager —— 解析遮罩（逐步显示）

**设计**：遮罩是覆盖在 PDF 画布之上的透明 `<canvas>`，擦除 = 在遮罩 canvas 上以 `globalCompositeOperation = 'destination-out'` 绘制，露出下方内容。三种模式只是「擦除方式」不同，底层共用一套绘制逻辑。

```typescript
class MaskManager {
  /** 每页持久擦除笔画（相对页面坐标，缩放无关） */
  pages = new Map<number, MaskStroke[]>();
  mode: 'click' | 'hover' | 'eraser';

  /** 点击模式：固定半径圆形擦除 */
  eraseCircle(page: number, x: number, y: number, r = 60): void;

  /** 橡皮擦：mousemove 连续折线擦除 */
  erasePolyline(page: number, points: Point[]): void;

  /** 悬停：临时擦除，移开恢复（不写 pages） */
  renderHover(ctx: CanvasRenderingContext2D, x: number, y: number, r = 80): void;

  /** 重绘某页遮罩（仅持久笔画 + 当前悬停态） */
  renderPage(canvas: HTMLCanvasElement, page: number, scale: number): void;

  clearPage(page: number): void;   // 一键恢复该页遮罩
  clearAll(): void;
}
```

**关键点**：
- 笔画坐标存「相对页面」坐标，渲染时乘以当前 `scale` 映射到 canvas 像素——缩放后擦除位置仍然正确。
- 遮罩状态持久化到内存（每页笔画列表），翻页再回来仍保持已擦区域；「一键显示/隐藏整页解析」= 整体开关遮罩渲染。
- 只对**可视页**渲染遮罩 canvas（与懒加载同生命周期），避免内存浪费。

---

### 6.4 PdfDocument —— PDF.js 封装

```typescript
class PdfDocument {
  pdf: PDFDocumentProxy;
  static async load(src: ArrayBuffer | string): Promise<PdfDocument>;  // 含密码处理

  async renderPage(pageIndex: number, canvas: HTMLCanvasElement, scale: number): Promise<void>;
  async getTextContent(pageIndex: number): Promise<TextContent>;       // 搜索/复制/OCR 扩展
  async getOutline(): Promise<OutlineNode[]>;                          // 目录联动
  get numPages(): number;
}
```

- 加载：`getDocument({ data })`，配 `GlobalWorkerOptions.workerSrc` 指向本地 worker（避免 CDN 依赖，Electron 离线可用）。
- 渲染：每页渲染到独立 canvas，作为 `PdfPage.vue` 的内容；DPI 适配通过 `scale = baseScale × devicePixelRatio` 实现高清无锯齿。
- 加密 PDF：捕获 `PasswordException`，弹出密码输入框后重试。
- 扫描版 PDF：文本层为空，搜索/复制自动降级（提示可开 OCR，P2）。

---

### 6.5 分页懒加载 + 虚拟滚动（useVirtualScroll）

- 滚动容器高度 = 所有页高度之和 + 页间距；每页用绝对定位 + `translateY(pageTops[i])` 摆放。
- 仅渲染 `[firstVisible - 2, lastVisible + 2]` 区间内的页（前后各 2 页预加载，滚动无白屏）。
- 滚出区间的页：卸载 canvas（释放 bitmap）、回收 DOM（或复用 DOM 池）。
- 用 `requestAnimationFrame` + 节流计算可视区间，避免高频重渲染。

> 内存收益：数百页 PDF 任意时刻只保留约 5 页的渲染位图，长时间多套试卷不占高内存。

---

### 6.6 目录与搜索联动

- **目录**：`getOutline()` 返回章节树 → `OutlinePanel.vue` 渲染。点击章节 → 主 viewer `gotoPage(p)` → 触发 SyncEngine → 从 viewer 跳到映射页。
- **全局搜索**：一次输入，分别在两个 `PdfDocument` 上 `getTextContent()` 检索（可预提取全文建索引提升大文件速度）。结果两侧高亮；点击某侧结果 → 双侧同步跳转定位。

---

### 6.7 文件配对记忆

- **手动配对为主**：用户直接为左侧题本选择对应解析（或反向），命名不规律也能用。
- **自动配对辅助**：仅当文件名符合规则（如 `XXX_题本.pdf` ↔ `XXX_解析.pdf`）时，批量导入文件夹可自动提取公共前缀成组；无法识别则留给手动。
- **记忆**：`FilePair` 存数据目录 `pairs.json`；打开题本时若命中配对记录，自动加载解析到另一侧；启动页展示历史配对，一键恢复进度。
- **跨机失效处理**：配对中的 PDF 路径失效时，走 8.5 节的文件重定位机制。

---

### 6.8 标注层（画笔 / 文本框）——P0 已提前实现

**层级**：PDF canvas < 遮罩 canvas < 笔迹 canvas < 文本框 DOM（标注永远显示在遮罩之上，自己做的笔记不被遮罩盖住）。

**数据模型**：标注按「文件路径 → 页 → {strokes, boxes}」组织，存数据目录 `annotations.json`（原子写）。笔画/文本框坐标均存「相对页面的基础坐标」（uiScale=1 时的 CSS px），缩放后位置不变，渲染时乘 `uiScale × dpr`。

**工具路由**（`activeTool: browse / pen / text`）：
- `browse`（默认）：滚动 + 遮罩交互（点击/悬停/橡皮擦照旧）；
- `pen`：按下拖动画笔，可选颜色（色板）与粗细（3 档），实时预览当前笔画。**不提供拖动擦除**（实测拖动擦除因全局版本号触发所有渲染页 canvas 重绘而卡顿，已按用户决定移除），画错用撤销处理；
- `text`：点击页面创建文本框，输入后 Enter 保存，双击编辑、拖动移动、悬停出现删除按钮。

**撤销（Ctrl+Z / 工具栏「撤回」）**：会话内操作历史栈（上限 100 条），覆盖画笔添加、文本框增/删/改；空框不计入历史；一次拖动擦除合并为单条记录（显式会话：mouseup 结束，无时间窗口歧义）。文本框编辑聚焦时 Ctrl+Z 不拦截（走原生文本撤销）。**撤销即「擦」**：画错的笔迹通过撤回删除。

**文本框生命周期**：创建 → 编辑态（textarea 自动聚焦）→ 提交（空文本自动删除）/ Esc 取消。**空框四层防护**（blur 在虚拟滚动/快速切工具场景不可靠）：① blur 提交删除；② 连续创建时先结算上一个编辑框；③ 工具切换 watch(activeTool) 结算；④ 组件卸载时结算；另加数据层兜底——`toJSON()` 过滤空文本框，空框永不落盘。拖动采用 **Pointer Events + setPointerCapture**（pointerup 保证触发），配合 `user-select: none` 与 `preventDefault`，杜绝原生文本选择吞掉 mouseup 导致文本框持续跟随鼠标的问题。

**持久化**：`annotations.json` 为 `DocAnnotations[]`（path/fileName/pages），与书签同走原子写 + 同步中心合并。

**悬停遮罩形状自定义**：`settings.json` 新增 `hoverShape: circle | square | wide | tall`（圆形/方形/横条/竖条），hover 模式下工具栏可选择，`MaskManager.renderPage` 按形状绘制（circle 为圆弧，其余为圆角矩形）。

---

## 7. IPC 协议（主进程 ↔ 渲染进程）

安全模型：`contextIsolation: true`、`nodeIntegration: false`，通过 `contextBridge` 暴露白名单 API。

```typescript
// preload 暴露 window.api
interface Api {
  // 文件
  openPdfDialog(): Promise<string | null>;         // 选单个 PDF
  openFolderDialog(): Promise<string | null>;      // 批量导入
  readPdfInfo(path: string): Promise<{ size: number; mtime: number }>;

  // 本机偏好（electron-store）
  getSettings(): Promise<AppSettings>;
  setSettings(s: Partial<AppSettings>): Promise<void>;

  // 快捷键
  onShortcut(cb: (action: string) => void): void;
}
```

> 说明：业务数据（书签/进度/配对等）以 JSON 文件形式存于可自定义「数据目录」，原子写保证完整性，便于跨机同步；本机偏好（窗口状态、数据目录路径）走 electron-store。详见第 8 节。

---

## 8. 数据存储与双机同步设计

### 8.1 存储分层（存储与同步解耦）

| 层 | 位置 | 内容 | 是否跨机同步 |
|----|------|------|-------------|
| 业务数据 | 数据目录 `data/`（用户可自定义路径） | `pairs.json`、`bookmarks.json`、`progress.json`、`mistakes.json`、`annotations.json`、`settings.json`(可同步偏好) | ✅ |
| 本机偏好 | electron-store | 窗口位置/大小、数据目录路径、最近打开 | ❌（每机独立） |

### 8.2 数据文件格式（原子写 + 可合并）

- 每个文件 = 记录数组，每条记录含 `id`(UUID) 与 `updatedAt`(毫秒时间戳)。
- **原子写**：先写 `xxx.json.tmp`，再 `rename` 覆盖 `xxx.json`，任何时刻文件都是完整的（手动拷贝/网盘不会拿到半截文件）。
- **合并规则**：导入时按 `id` 去重，同 `id` 取 `updatedAt` 较新者（单人双机足够）。

### 8.3 同步中心（手动同步，适配夸克网盘）

夸克网盘**不支持双向文件夹同步**，也**无官方 WebDAV / 开放 API**（仅单向「自动备份」上传）。因此采用**应用内「同步中心」手动导出/导入**，夸克只当「文件仓库」中转。

**入口**：工具栏固定一个「同步」按钮，点击弹出「同步中心」对话框，包含两个操作：

**① 导出备份（生成同步文件）**
- 一键把整个 `data/`（含 JSON 与错题截图等）打包为 `backup-<yyyyMMdd-HHmmss>.zip`。
- 默认存到用户上次选择的导出位置（记住路径），也可另存。
- 完成后提示文件路径，引导「上传到夸克网盘」。

**② 导入备份（上传同步文件同步数据）**
- 点击「导入」选择 `.zip`，或直接把 `.zip` 拖入应用窗口。
- 校验备份结构 → 解压到临时目录 → 按 8.2 规则与当前数据合并（`id` 去重、`updatedAt` 取新）。
- 完成后弹合并报告：`新增 X 条 · 更新 Y 条 · 无变化 Z 条`，并刷新界面。

**合并安全**：先在内存合并，落盘仍走原子写（先 `*.tmp` 再 rename），中途失败不影响原数据。

> **扩展性**：因数据是稳定 JSON 文件，若用户改用支持双向同步的网盘（坚果云 / OneDrive / Dropbox），只需把「数据目录」指向该网盘的本地同步文件夹，即获得**自动实时同步**（零额外开发）。内置 WebDAV 同步列为 P2 可选。

### 8.4 IPC 补充

```typescript
interface Api {
  getDataDir(): Promise<string>;
  setDataDir(p: string): Promise<void>;

  // 文件重定位
  locateFile(fileName: string): Promise<string | null>;   // 路径失效时按文件名搜索/手动定位

  // 同步中心
  exportBackup(): Promise<{ path: string; files: number } | null>;  // 打开另存对话框并打包
  importBackup(): Promise<MergeReport | null>;                       // 打开文件选择并合并
}

interface MergeReport {
  added: number;      // 新增记录数
  updated: number;    // 覆盖更新数
  unchanged: number;  // 无变化数
}
```

### 8.5 文件定位与重定位（跨机路径失效处理）

业务数据（配对/书签/进度）存的是 PDF 的**绝对路径**，双机路径不一致时导入后会失效。因此记录里存 `FileRef { path, fileName }`，加载时按以下顺序定位：

1. **直接命中**：`path` 存在 → 直接用。
2. **按文件名搜索**：在「已知 PDF 目录列表」里按 `fileName` 查找（浅层扫描，不递归全盘）。
   - 「已知目录」自动积累：每次成功打开 PDF，记录其所在目录；也允许用户手动添加搜索目录。
3. **手动定位**：仍未找到 → 弹文件选择框让用户手动指向，成功后更新 `FileRef.path`（缓存）。

> 命中后 `FileRef.path` 被回写更新，下次直接命中；书签/进度通过 `pairId` 关联配对，配对定位成功后其余数据自动恢复。

---

## 9. 状态管理（Pinia）

| Store | 职责 | 关键 state |
|-------|------|-----------|
| `viewerStore` | 两侧 viewer 的加载态、页数、当前页浮点、缩放、滚动位置 | `left/right: ViewerState` |
| `syncStore` | 同步模式、倍率/偏移/锚点、主从方向 | `SyncSettings` |
| `maskStore` | 遮罩模式、每页笔画状态、整页显隐开关 | `mode`, `pages` |
| `bookmarkStore` | 书签增删改查、按标签筛选 | `bookmarks` |
| `pairStore` | 当前配对、历史配对列表 | `currentPair`, `pairs` |
| `settingsStore` | 主题、布局、快捷键、缩放联动 | `AppSettings` |

---

## 10. UI 交互设计要点

### 10.1 双栏布局（SplitPane.vue）

- 左右宽度比例可拖动（中间分隔条 `mousedown` + 拖拽）。
- 双击分隔条 → 均分 50/50。
- 三种布局切换：`split` / `questionOnly`（隐藏解析，专注做题）/ `answerOnly`。
- 专注模式下「呼出解析栏」= 切回 split，并自动同步到当前题本页对应的解析位置。

### 10.2 顶部工具栏自动隐藏

- 工具栏绝对定位顶部，鼠标移到屏幕顶部边缘触发滑入；移出后延时滑出。
- 全屏时配合 `requestFullscreen` / 快捷键退出。

### 10.3 主题（useTheme）

- 4 主题：`day`/`night`/`warm`/`print`，通过 CSS 变量切换（工具栏、面板、背景色）。
- 解析页「亮度/对比度」：对解析 canvas 叠加 `filter: brightness()/contrast()`。
- 跟随系统主题：`matchMedia('(prefers-color-scheme: dark)')`。

### 10.4 快捷键（useShortcut）

- 内置默认表（翻页、缩放、全屏、显示/隐藏解析、切换遮罩、加书签、呼出草稿板、一键对齐），全部可在设置里改绑。
- 鼠标侧键：监听 `MouseEvent.button === 3/4`（`mousedown` + `preventDefault`）映射到高频操作。

---

## 11. 关键技术难点与对策

| 难点 | 对策 |
|------|------|
| 双栏滚动互相触发抖动/死循环 | 主从单向监听防回环；scroll 事件按帧派发天然节流（同步应用） |
| 缩放后擦除位置错位 | 遮罩笔画存「相对页面」坐标，渲染时乘 scale |
| 大文件卡顿/内存高 | 虚拟滚动只渲染可视页 ±2，滚出即释放 bitmap |
| 扫描版 PDF 无法搜索 | 文本层为空时降级提示，P2 接 OCR |
| 加密 PDF | 捕获 `PasswordException` 弹密码框重试 |
| DPI 缩放模糊 | canvas 按 `scale × devicePixelRatio` 渲染，CSS 尺寸归一 |
| 目录跳转后两侧不对齐 | 目录跳转统一走 SyncEngine 映射，而非各自独立跳页 |

---

## 12. 分阶段实施计划

### P0 MVP（核心联动 + 刷题基础）
1. 脚手架：electron-vite + Vue3 + TS + Pinia + pdfjs-dist 打通双栏打开
2. PageMapper + SyncEngine（比例同步 + 主从）+ 缩放同步
3. MaskManager（点击/悬停/橡皮擦）+ 一键显隐
4. 数据目录 + JSON 存储骨架（原子写，为书签/进度落盘打底）
5. 书签（双侧位置 + 标签 + 侧边栏筛选）
6. 虚拟滚动懒加载 + 4 主题

### P1 二期（效率优化）
7. pageScale / anchor 同步 + 一键对齐 + 自动估算倍率
8. 目录 / 全局搜索联动
9. SplitPane 拖拽 + 布局切换 + 专注模式 + 草稿板
10. 快捷键 + 侧键映射 + 文件配对记忆 + 启动页
11. 同步中心（「同步」按钮导出备份 + 导入备份合并报告）

### P2 进阶（知识沉淀）
12. 双侧标注（高亮/下划线/备注/矩形，双向关联）
13. 错题本（框选截图 + 导出 PDF/图片）
14. OCR（扫描版）+ 阅读进度统计

---

## 13. 风险与待确认项

1. **版本锁定**：Electron / pdfjs-dist / electron-vite 需在搭建脚手架时锁定到当前最新稳定版（本稿以 4.x / 最新稳定为准，落地时核对）。
2. **文本搜索性能**：超大 PDF 的全局搜索需预提取文本建索引（P1），否则逐页 `getTextContent` 有延迟。
3. **OCR 引擎**：已定本地引擎 **Tesseract.js（wasm）**，离线可用、免隐私上传；中文需加载 `chi_sim` 语言包（首次约 15~30MB）。
4. **错题本截图格式**：已定——内部存 **PNG**（无损、文字公式清晰）；错题集导出 **PDF 为主 + PNG 打包 zip 为辅**；截图按 `scale ≈ 2~3 × devicePixelRatio` 渲染，打印/放大不糊。
5. **数据量增长与迁移**：当前 JSON 文件方案足够；若错题本/标注量大到需要全文检索，再评估迁移 SQLite（P2 前决策）。

---

## 14. 下一步

文档评审通过后，即可按 P0 顺序搭建脚手架，产出可运行的「双栏打开 + 比例同步 + 遮罩」最小可用版本。
