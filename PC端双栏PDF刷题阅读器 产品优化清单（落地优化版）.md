- # PC端双栏PDF刷题阅读器 产品优化清单

  

  ## 一、核心联动体验（MVP必做，双栏产品核心壁垒）

  ### 1. 三层式轻量同步滚动（核心替换方案）

  #### （1）默认基础：滚动比例同步（零配置，开箱即用）

  - 逻辑：不绑定页码，按文档整体滚动高度的百分比进行同步。例如题本滚动到全书20%的位置，解析自动同步到自身20%的位置
- 优势：完全无需用户设置，打开文件默认生效，适合整本题目与解析篇幅比例大致均匀的教辅，大部分场景下能对齐到对应题目区域
  - 适配：天然兼容「解析比题目长」的情况，不会出现页码对不上的问题，仅在单章节篇幅差异极大时会出现小幅错位
  
  #### （2）进阶微调：页码倍率+起始偏移（一次设置，整本生效）

  - 逻辑：针对整套教辅的整体篇幅比例，提供两个极简设置项，设置一次全程生效：
  - **页码倍率**：例如解析总页数是题本的1.6倍，则设置倍率为1.6，题本每翻1页，解析自动翻1.6页
    - **起始偏移**：例如题本第1页对应解析第3页（前面有封面、目录），设置偏移量+2即可
  - 操作成本：整套教辅只需要设置1次，耗时不超过10秒，对齐精度远高于纯比例同步，足够覆盖整本书的刷题需求
  - 补充：支持自动估算倍率，用户手动对齐第一页和最后一页，系统自动计算出倍率与偏移，无需手动算数
  
  #### （3）即时校准：一键对齐当前位置（随手修正错位）

  - 逻辑：当滚动到某道题出现错位时，用户手动将两侧都调整到当前题目的起始位置，按下快捷键/点击按钮，系统将此位置设为新的同步基准，后续滚动按新的比例自动同步
- 优势：无需提前规划，遇到错位随手修正，一次操作管后面所有内容，完全符合刷题「从前往后做、遇到问题再调整」的线性使用习惯
  - 操作成本：单次1次按键/点击，无学习成本
  
  #### （4）主从同步模式（符合刷题操作逻辑）

  - 默认设置「左题本为主窗口，右解析为从窗口」：只有操作左侧题本滚动/翻页时，右侧解析才跟随同步；单独滚动右侧解析时，左侧题本位置保持不变
- 价值：对答案时滑动解析不会打乱题本的阅读位置，避免反向联动干扰做题节奏，更符合「主动做题、被动看答案」的使用习惯
  - 支持切换主从方向，满足不同用户的布局习惯
  
  ### 2. 缩放与视图同步

  - 单侧调整缩放比例时，支持双侧同步跟随，也可独立缩放，适配题目与解析不同的内容密度
- 旋转视图、单页/连续滚动模式切换，支持双侧同步生效，也可分别独立设置
  
  ### 3. 目录与搜索联动

  - 点击任意一侧PDF的目录章节，两侧同时跳转到对应章节起始位置
- 全局关键词搜索：一次输入同时匹配两个PDF，结果双侧高亮，点击结果双侧同步跳转定位
  
  ## 二、刷题场景专属功能（MVP必做，核心差异化价值）

  ### 1. 解析遮罩与逐步显示

  - 默认给解析页覆盖半透明遮罩，避免做题时提前瞟到答案
- 三种显示模式自由切换：
    - 点击区域显示：鼠标点击哪块区域，哪块区域遮罩消失
    - 悬停显示：鼠标悬停位置临时显示答案，移开自动恢复遮罩
    - 橡皮擦模式：手动擦除对应题目的遮罩，只显示当前核对的题目，不泄露后续答案
  - 支持一键显示/隐藏整页解析，可绑定快捷键快速切换
  
  ### 2. 专注做题模式

  - 一键隐藏解析栏，全屏仅显示题本页面，最大化做题视野
- 做完后一键呼出解析栏，自动定位到当前页面对应的解析位置
  - 内置侧边草稿板：从题本边缘呼出空白草稿区域，支持打字输入、简单图形标注，无需额外打开记事本打断思路
  
  ### 3. 快速标记书签

  - 一键添加书签，自动关联双侧当前位置，点击书签双侧同步跳转
- 支持快速打标签（错题、重点、存疑），无需复杂命名，满足一次性刷题的快速标记需求
  - 书签列表侧边栏展示，支持按标签筛选，方便做完后集中复盘错题
  
  ## 三、交互效率优化（二期优化，降低操作成本）

  ### 1. 分栏布局自由调节

  - 中间分隔条支持自由拖动，自定义左右窗口宽度比例
- 双击分隔线一键均分双侧宽度
  - 预设3种布局一键切换：左右分栏、仅显示题本、仅显示解析
  
  ### 2. 全场景快捷键覆盖

  - 基础操作：翻页、缩放、退出全屏，兼容主流PDF阅读器快捷键习惯
- 专属功能：显示/隐藏解析、切换遮罩、添加书签、呼出草稿板、即时对齐同步，全部支持自定义
  - 支持鼠标侧键映射，将翻页、显示答案等高频操作绑定到鼠标侧键
  
  ### 3. 文件配对记忆

  - 打开过的题本与解析PDF自动绑定配对，下次打开题本时，自动加载对应解析到另一侧
- 支持按文件名规则自动配对（如`XXX_题本.pdf`与`XXX_解析.pdf`），批量导入文件夹时自动成组
  - 历史配对记录在启动页展示，一键恢复上次刷题进度
  
  ## 四、视觉与长时间使用体验（二期优化，提升使用舒适度）

  ### 1. 多主题护眼模式

  - 内置4种显示主题：日间纯白、夜间深色、护眼米黄、纯黑白印刷模式，一键切换
- 解析页支持单独调整亮度/对比度，避免答案区域过于刺眼、分散注意力
  - 支持跟随系统主题自动切换日间/夜间模式
  
  ### 2. 高清渲染与屏幕适配

  - 矢量PDF高清渲染，文字、公式、图表放大无锯齿，适配2K/4K高分辨率屏幕
- 适配系统DPI缩放，界面、按钮、文字不会出现模糊、错位、尺寸过小问题
  
  ### 3. 沉浸式极简界面

  - 顶部工具栏支持自动隐藏，鼠标移到屏幕顶部才呼出，最大化PDF显示区域
- 精简非核心界面元素，核心功能按钮突出，减少视觉干扰
  
  ## 五、性能与稳定性（基础保障，MVP必做）

  ### 1. 大文件流畅加载

  - 分页懒加载机制：数百页PDF秒开，仅渲染当前页及前后2页内容，无需等待全本加载
- 预加载策略：滚动时提前加载下一页内容，翻页无白屏、无卡顿
  
  ### 2. 低内存占用

  - 自动释放未浏览页面的渲染缓存，长时间打开多套试卷不会占用过高内存导致系统卡顿
- 多标签页时后台标签自动休眠，仅保留当前使用的一组PDF渲染资源
  
  ### 3. 全格式兼容

  - 兼容标准矢量PDF、扫描版PDF、加密PDF（输入密码后可打开）、带原生注释的PDF
- 可选OCR能力（进阶扩展）：对扫描版PDF做文字识别，解锁文字搜索、复制、标注能力
  
  ## 六、学习辅助与知识沉淀（进阶扩展，按需实现）

  ### 1. 双侧关联标注

  - 题本与解析页均支持高亮、下划线、文字备注、矩形标注
- 标注双向关联：在题本上标记的内容，自动在解析对应位置生成关联标记，点击可互相跳转
  
  ### 2. 一键错题收录

  - 框选题本区域+对应解析区域，一键加入错题本，自动保存题目截图、解析截图与个人笔记
- 错题本支持分类、打标签、关键词搜索，可按科目、章节整理
  - 支持导出错题集为PDF/图片格式
  
  ### 3. 阅读进度记忆

  - 自动记录每套题的最后阅读位置，下次打开直接恢复，无需重新翻页
- 可选学习数据统计：刷题页数、学习时长，辅助用户规划学习进度